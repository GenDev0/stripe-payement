import React from "react";

type Props = {
  setcartSliderIsOpen: React.Dispatch<React.SetStateAction<boolean>>;
};

export default function Header({ setcartSliderIsOpen }: Props) {
  return <div>Header</div>;
}
